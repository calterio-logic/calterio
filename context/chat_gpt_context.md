You are building a reusable enterprise web design system.
Use React + TypeScript + Tailwind.
No marketing fluff. No gradients.
Dark theme by default.
Create a Hero component with:
- headline
- subtext
- primary & secondary CTA
- architecture visual slot
Follow system software aesthetics.


Generate a Next.js SEO use-case page that:
- Accepts YAML input
- Enforces exactly one H1
- Generates schema.org JSON-LD
- Uses UWDS components only
- Fails build if meta description missing


Generate SVG architecture diagrams using only:
- rectangles, rounded rectangles, circles
- solid or dashed lines
- monochrome base + single accent color
No gradients, no icons, no images.

Generate a landing page using best practices.
Tone: enterprise systems, not marketing.
Show architecture, metrics, and integrations.
No testimonials. No stock images.


We want:
⚡ <300ms load time

🧠 Google sees topical authority, not blogs

🏗️ Websites feel like operating manuals

🔁 Generate  strong urge buy

We wil be deploying the site to GitHub Pages for the Free hosting for static HTML/CSS/JS sites directly from a GitHub repo. And we will enable custom domains with HTTPS. No server logic. 